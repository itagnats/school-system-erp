import type { Enrollment } from "@/types";

/**
 * Fictional enrollments fixtures. Populated when the module is implemented.
 * Demo data only: never real student or employee information (scaffold.md §12).
 */
export const mockEnrollments: Enrollment[] = [];
