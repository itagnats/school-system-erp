export { FormSection, FormFieldWide } from "./form-section";
export { FormActions } from "./form-actions";

// The React Hook Form bindings themselves live with the primitives, since the
// shadcn contract expects them at components/ui/form.
export {
  Form,
  FormField,
  FormItem,
  FormLabel,
  FormControl,
  FormDescription,
  FormMessage,
  useFormField,
} from "@/components/ui/form";
